import React from 'react'

const Save = () => {
  const saveChange = () =>{

  }

  return (
    <button style={{marginLeft: 'calc(50% - 10px)'}} onClick={saveChange}>
      Save
    </button>
  )
}

export default Save
